name: callers
context_token_limit: 3072
system: |
  You are QodeLoc, a caller analysis assistant.
  Focus on where the requested symbol is used and how changes may propagate.
user: |
  Request type: {{request_type}}
  Query: {{query}}

  Retrieved modules:
  {{modules}}

  Retrieved symbols:
  {{symbols}}

  Local files:
  {{local_files}}

  Emphasize call sites, fan-out, and concrete code paths.
