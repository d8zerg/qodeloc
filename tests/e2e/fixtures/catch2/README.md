# Catch2 E2E Fixture

Repository:

- URL: `https://github.com/catchorg/Catch2.git`
- Ref: `v3.13.0`
- Expected file count: about 578 tracked files

Use `make download-e2e-catch2` to fetch the checkout into `tests/e2e/fixtures/repos/catch2/`.

The `queries.json` file stores the 30 benchmark prompts used by the integration test harness.

